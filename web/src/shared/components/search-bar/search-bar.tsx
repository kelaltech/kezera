import React, { FormEventHandler, useEffect } from 'react'import { Block, Content, Flex, Input } from 'gerami'import { IContentProps } from 'gerami/src/components/Content/Content'import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'import useField from '../../hooks/use-field/use-field'type Props = IContentProps & {  onTerm?: (term: string) => void  onSearch?: FormEventHandler<HTMLFormElement>}function SearchBar({ onTerm, onSearch, placeholder, ...contentProps }: Props) {  const term = useField<HTMLInputElement>({    setValueHook: async value => {      onTerm && onTerm(value)      return value    }  })  useEffect(() => {    const timeout = setTimeout(() => term.ref.current && term.ref.current.focus(), 1000)    return () => clearInterval(timeout)  }, [])  return (    <Content {...contentProps}>      <label>        <Block className={'padding-bottom-big padding-horizontal-big'}>          <Flex>            <div className={'margin-top-auto padding-right-big fg-accent'}>              <FontAwesomeIcon icon={'search'} />            </div>            <form className={'margin-vertical-auto full-width'} onSubmit={onSearch}>              <Input                {...term.inputProps}                inputRef={term.ref}                placeholder={placeholder || `Search for Organization Applications`}                className={'full-width'}                type={'search'}              />            </form>          </Flex>        </Block>      </label>    </Content>  )}export default SearchBar